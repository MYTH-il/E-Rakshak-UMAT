[Unit]
Description=BinderFS for UMAT ReDroid workers
After=systemd-modules-load.service
Before=docker.service umat-android-executor.service

[Mount]
What=binder
Where=/dev/binderfs
Type=binder

[Install]
WantedBy=multi-user.target
