# C2 deployment patches

Runtime `970e941-umat.1` uses the reachable upstream commit `970e941`. The ThreatFox
offline importer and completeness gate are included directly in upstream `970e941`,
so no additional deployment patch series is required.
