[Service]
# The database is deterministically rebuilt from feeds in the verified runtime.
Environment=THREATINTEL_DB=/srv/winstdt/c2-data/threatintel.sqlite
# Permit SQLite and its journal files to update outside the read-only runtime.
ReadWritePaths=/srv/winstdt/c2-data
